# Testovaci profil – datasource dodava Testcontainers cez @ServiceConnection,
# preto sa tu URL nekonfiguruje.
spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

logging:
  level:
    sk.martin.freelancepilot: DEBUG